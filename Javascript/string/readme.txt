string:
- extract char by char: use a[i]
- if any character is unable to find it throughs Undefined

functions in array:
- .length: used to show total length



*template literals: `` and ${}
short hand of using or printing strings like fstrings in python
you may use "" insde back 

*escape sequence
\n- new line
\t- new tab
\r- carriage return

functions:
.toUpperCase()
.toLowerCase()
.length
.slice(start,end)- return sliced string
.replace(string, value)- replace the 1st occurance
.concat(new, multiple values)- concatinate two strings
.trim()- to remove white spaces
.charAt(index)- give the char at that index ** if no char fornd at that point, it shows '' blank string
.indexOf(char)- return char at that index ** if not present, it return -1
.startsWith(char) -return true false if satisfied
.endsWith(char) -return true false if satisfied

*** string is immutable
you may use "" insde back 

*escape sequence
\n- new line
\t- new tab
\r- carriage return

functions:
.toUpperCase()
.toLowerCase()
.length
.slice(start,end)- return sliced string
.replace(string, value)- replace the 1st occurance
.concat(new, multiple values)- concatinate two strings
.trim()- to remove white spaces
.charAt(index)- give the char at that index ** if no char fornd at that point, it shows '' blank string
.indexOf(char)- return char at that index ** if not present, it return -1
.startsWith(char) -return true false if satisfied
.endsWith(char) -return true false if satisfied

*** string is immutable
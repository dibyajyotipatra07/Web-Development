funtion:
- to define we use function keyword:
- used to perform a certian task multiple times
- return keyword is used to return the result to the function
- you may use default value in function definition to use that default value in code
- if any value is not parametrised, then it is returned as NaN(Not a Number) as the paraeter is Undefined
- arrow function is a short hand code of a function

syntax:
 const function = ()=>{
    
 }
transition:
    go from 1 stage to another smoothly

    it consists 4 attribute
        transition-property: on which the property is applied
        transition-duration: how many time
        transition-timing-function: how
            ease-in-out etc
        transition-delay: after how many time it start

transition-property: all for all elements to transition
transition-property: you can add multiple property by add them with comma separate

use all in one property
transition: property duration function delay;
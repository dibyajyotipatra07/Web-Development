define global variable:
it helps in multi implement code with one variable

:root{
    --varname:value;
}
to call var()

define local variable:
simply --varname in attributes

opacity: for opacity


--varname(value, vlaue2): value 2 for if not found

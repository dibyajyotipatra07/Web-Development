overflow propert:
can be set to auto,if overflow then scroll otherwise dont
can be set to scroll, scroll always

white-space: nowrap wrapping off, text shown in one line
if here i add text-overflow: ellipsis, then it will show dotd

if we want to overflow in any direction omly: select overflow-x/y
can be written in one line
overflow: x y;
Sizing Units
*pixel (px)
*viewport Width (vw) takes the unit wrt the screen
*viewport height (vh) takes the height unit wrt screen
*(em) the size it inherits it multiplied by em
*(rem) take the mutiplied sixe of root element (html)
*Vmin for lap -height or vh, for phn width or vw
*Vmax for lap -width or vh, for phn height or vw
*% takes the that percentage part of the parent


margin: auto -to center block element(it add auto margin to it)
min-width, min-height for min vaqlue of height and width
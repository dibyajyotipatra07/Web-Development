to add css to html:
-inline * use inside the tag as attribute, not a good practice
-internal * use inside the header tags
-external * write a different css file for html code efficiency and link it to the html through link tag